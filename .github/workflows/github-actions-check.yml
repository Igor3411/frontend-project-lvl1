name: GitHub Actions Check
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x, 15.x]
    steps:
      - name: npm install
        run: |
          make install
      - name: Run linter
        run: |
          make lint