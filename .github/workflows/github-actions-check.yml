name: GitHub Actions Check
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: |
          make install
      - name: Run linter
        run: |
          make lint